import React from 'react';

const Ejm4 = (props) => {

    
    return (
        <div>
            <h2>Nombre: {props.name}</h2>
            {props.children}
        </div>
    );
}

export default Ejm4;
